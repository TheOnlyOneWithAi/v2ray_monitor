const nodesEl=document.querySelector('#nodes'),refreshButton=document.querySelector('#refresh'),updatedEl=document.querySelector('#updated'),accessEl=document.querySelector('#access'),searchEl=document.querySelector('#search'),addButton=document.querySelector('#add'),dialog=document.querySelector('#dialog'),form=document.querySelector('#configForm');
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function latency(v){if(v==null)return '—';const n=Number(v);return Number.isFinite(n)?`${Math.round(n)} ms`:'—'}function state(s){return s==='online'?'🟢':s==='offline'?'🔴':'🟡'}
function init(){if(window.Telegram?.WebApp){Telegram.WebApp.ready();Telegram.WebApp.expand()}}
function headers(){const h={'Accept':'application/json'};const d=window.Telegram?.WebApp?.initData||'';if(d)h['X-Telegram-Init-Data']=d;return h}
async function load(){refreshButton.disabled=true;try{const r=await fetch('/api/configs?q='+encodeURIComponent(searchEl?.value||''),{cache:'no-store',headers:headers()});if(r.status===401||r.status===403||r.status===402){accessEl.textContent='🔒 اشتراک پایش فعال نیست یا از طریق بات Seller وارد نشده‌اید.';nodesEl.innerHTML='';return}if(!r.ok)throw Error(`HTTP ${r.status}`);accessEl.textContent='';const d=await r.json(),list=Array.isArray(d.configs)?d.configs:[];nodesEl.innerHTML=list.map(n=>`<article class="node"><div><strong>${esc(n.name)}</strong><small>${esc(n.protocol)}</small></div><div class="right"><b>${state(n.status)}</b><span>${latency(n.latency_ms)}</span></div></article>`).join('')||'<p class="empty">کانفیگی ثبت نشده است.</p>';if(updatedEl)updatedEl.textContent=`آخرین بروزرسانی ${new Date().toLocaleTimeString('fa-IR')}`}catch(e){nodesEl.innerHTML='<p class="error">Monitor temporarily unavailable.</p>'}finally{refreshButton.disabled=false}}
async function account(){try{const r=await fetch('/api/me',{headers:headers()});if(!r.ok)return;const d=await r.json();document.querySelector('#account').textContent=`اشتراک: ${d.used_configs} / ${d.max_configs} کانفیگ — انقضا: ${new Date(d.expires_at).toLocaleDateString('fa-IR')}`}catch(e){}}
addButton?.addEventListener('click',()=>dialog?.showModal());refreshButton.addEventListener('click',load);searchEl?.addEventListener('input',()=>load());form?.addEventListener('submit',async e=>{if(e.submitter?.id!=='save')return;e.preventDefault();try{const config=JSON.parse(document.querySelector('#config').value);const r=await fetch('/api/configs',{method:'POST',headers:{...headers(),'Content-Type':'application/json'},body:JSON.stringify({name:document.querySelector('#name').value,protocol:document.querySelector('#protocol').value,config})});if(!r.ok){const x=await r.json().catch(()=>({}));throw Error(x.detail||`HTTP ${r.status}`)}form.reset();dialog.close();load()}catch(err){alert(err.message)}});init();account();load();setInterval(load,10000);
