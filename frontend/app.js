async function load(){const r=await fetch('/api/nodes',{cache:'no-store'});const d=await r.json();document.querySelector('#nodes').innerHTML=d.nodes.map(n=>`<article><div><strong>${esc(n.name)}</strong><small>${esc(n.protocol)}</small></div><div class="right"><b class="${n.status}">${n.status==='online'?'●':'○'}</b><span>${n.latency_ms==null?'—':n.latency_ms+' ms'}</span></div></article>`).join('')||'<p>No nodes.</p>'}function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}document.querySelector('#refresh').onclick=load;load();setInterval(load,10000);
